server:
  port: 8082

spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${studentcourse.mysql.host}:${studentcourse.mysql.port}/db_course?${studentcourse.mysql.params}
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASSWORD:123888}
    hikari:
      pool-name: course-service-hikari
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000

logging:
  level:
    com.yun.studentcourse.course: info

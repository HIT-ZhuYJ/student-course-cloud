<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yun.studentcourse.teacher.mapper.TeacherMapper">

    <resultMap id="TeacherResultMap" type="com.yun.studentcourse.teacher.entity.Teacher">
        <id column="teacher_id" property="teacherId"/>
        <result column="teacher_no" property="teacherNo"/>
        <result column="name" property="name"/>
        <result column="title" property="title"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="TeacherColumns">
        teacher_id, teacher_no, name, title, phone, email, status, create_time, update_time
    </sql>

    <insert id="insert" parameterType="com.yun.studentcourse.teacher.entity.Teacher" useGeneratedKeys="true" keyProperty="teacherId">
        INSERT INTO teacher (
            teacher_no, name, title, phone, email, status
        ) VALUES (
            #{teacherNo}, #{name}, #{title}, #{phone}, #{email}, #{status}
        )
    </insert>

    <select id="findById" resultMap="TeacherResultMap">
        SELECT <include refid="TeacherColumns"/>
        FROM teacher
        WHERE teacher_id = #{teacherId}
    </select>

    <select id="findByTeacherNo" resultMap="TeacherResultMap">
        SELECT <include refid="TeacherColumns"/>
        FROM teacher
        WHERE teacher_no = #{teacherNo}
    </select>

    <select id="findPage" resultMap="TeacherResultMap">
        SELECT <include refid="TeacherColumns"/>
        FROM teacher
        <where>
            <if test="keyword != null and keyword != ''">
                AND (
                    teacher_no LIKE CONCAT('%', #{keyword}, '%')
                    OR name LIKE CONCAT('%', #{keyword}, '%')
                    OR title LIKE CONCAT('%', #{keyword}, '%')
                )
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        ORDER BY teacher_id DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(1)
        FROM teacher
        <where>
            <if test="keyword != null and keyword != ''">
                AND (
                    teacher_no LIKE CONCAT('%', #{keyword}, '%')
                    OR name LIKE CONCAT('%', #{keyword}, '%')
                    OR title LIKE CONCAT('%', #{keyword}, '%')
                )
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
    </select>

    <update id="update" parameterType="com.yun.studentcourse.teacher.entity.Teacher">
        UPDATE teacher
        SET name = #{name},
            title = #{title},
            phone = #{phone},
            email = #{email},
            status = #{status}
        WHERE teacher_id = #{teacherId}
    </update>

    <update id="disable">
        UPDATE teacher
        SET status = 'DISABLED'
        WHERE teacher_id = #{teacherId}
    </update>
</mapper>
